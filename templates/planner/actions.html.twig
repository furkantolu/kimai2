{% macro planner(view) %}
    {% import "macros/widgets.html.twig" as widgets %}

    {% set actions = {} %}

    {% set event = trigger('actions.planner', {'actions': actions, 'view': view}) %}
    {{ widgets.page_actions(event.payload.actions) }}
{% endmacro %}
